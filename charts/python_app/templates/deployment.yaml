apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "python_app.fullname" . }}
  labels:
    app: {{ include "python_app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "python_app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "python_app.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 5000
